# <your-org> backup schedules — managed by devops-infra repo
SHELL=/bin/bash
MAILTO=""

# Production PG backups daily at 04:00 CET (03:00 UTC in summer / 03:00 UTC in winter)
# Using CET timezone — 4am local time year-round
CRON_TZ=CET
0 4 * * * root . /opt/backups/.env && /usr/bin/python3 /opt/backups/scripts/backup_pg.py --production >> /opt/backups/logs/backup_pg.log 2>&1

# Config backups daily at 04:30 CET (after PG backups finish)
30 4 * * * root . /opt/backups/.env && /usr/bin/python3 /opt/backups/scripts/backup_configs.py >> /opt/backups/logs/backup_configs.log 2>&1
